# JwNavigator レイヤ制御 — 調査結果の引き継ぎ

作成: 2026-09-04 / 調査環境: Jw_cad 10.03.3 (C:\jww), Windows 11, Python 3.13

## 1. 何をやろうとしているか

Jw_cad の全レイヤグループ×全レイヤ（16×16＝256）の状態を

- **保存**（現在の状態を丸ごと記録）
- **復元**（記録した状態に戻す）

を、それぞれ数秒でできるようにする。JwNavigator に組み込むのが最終目標。

当初は「レイヤを1つずつ切り替えて画面情報をコピーし、状態を調べる」という
手作業（256×4≒1000回）をしていたが、それは不要になった。

## 2. 到達点

| 項目 | 状態 |
|---|---|
| 256レイヤの状態取得 | **完了**（外部変形1回、実機で `group=16 layer=256` 確認済み） |
| 状態値の意味の特定 | **完了**（公式仕様書に定義あり） |
| 外部変形での状態の書き戻し | **不可と実証**（下記4-4） |
| レイヤ設定ファイル(.JWL)での復元 | **完了**（WM_COMMAND 32923 で自動化まで実機確認済み） |
| `hr` 自動ループでの無操作保存 | **破棄**（11章。図面状態を破壊するため） |
| 起動の短縮（Ctrl+A） | **未適用**（手順は4-6。JWFは元に戻した） |

## 3. 一次情報の在り処

- `C:\jww\JWW_SMPL.BAT` … Jw_cad 同梱。**外部変形の仕様書**（2007年時点）。
  制御行、書き出しデータ、読み込みデータの全定義がコメントで書かれている。
- `C:\jww\SAMPLE.JWL` … **レイヤ設定ファイルの仕様書**。値の定義が書かれている。
- `C:\jww\Sample.jwf` … 環境設定ファイルの仕様書。`GCOM_*` と キー割付の定義。
- `C:\jww\Jw_cad.chm` … ヘルプ。操作手順は載っているがデータ仕様は無い。
  変更履歴（VersionHistory.html）に機能追加の経緯がある。

**Web の解説サイトは誤りが混じっていた**（`#gn` の意味など）。
一次情報は上記のローカルファイルを見ること。

## 4. 確定した仕様

### 4-1. 外部変形バッチの制御行（256レイヤを取るための必須条件）

```
REM #jww     これが無いと外部変形の「ファイル選択」一覧に表示されない
REM #h1      データ選択方式。これが無いと #g1 が効かない
REM #g1      必ず #h* の直後に置く。全レイヤグループ選択
REM #gn      レイヤの状態と名前を書き出す。これが無いと何も出ない
REM #e
```

3条件すべてが揃って初めて256レイヤが出る。実機での検証結果：

| 制御行 | 結果 |
|---|---|
| `#gn` `#g1`（`#h*` なし） | group=1 layer=16 … `#g1` が無効 |
| `#h0` `#g1`（`#gn` なし） | group=0 layer=0 |
| `#h4` `#g1`（`#gn` なし） | group=0 layer=0 |
| `#g1` のみ | group=0 layer=0 |
| `#h1` `#g1`（`#gn` なし） | group=0 layer=0 |
| `#h4` `#g1` `#gn` | group=1 layer=16 … 選択しないと `#g1` 無効 |
| **`#h1` `#g1` `#gn`** | **group=16 layer=256** ✅ |

`#h*` の種類（`JWW_SMPL.BAT` より）:

- `#h0` データ選択をしない、データ書き出し無し
- `#h1` 範囲内のデータのみを選択
- `#h2` 範囲内データ・範囲内外に跨る線データを選択（かすめるだけで当たる）
- `#h3` 範囲内の「文字」データのみを選択
- `#h4` データ選択をしない、読取可能データ書き出し

### 4-2. 書き出しデータ（`jwc_temp.txt`）の形式

`C:\jww\jwc_temp.txt` に出る（バッチのフォルダではない）。文字コードは Shift_JIS。

```
hq
...ヘッダ...
lgf          ← 値なし = 書込レイヤグループ
ly0          ← 値なし = 書込レイヤ
...
#
...選択された図形データ...
#
lg0 11       ← レイヤグループ0の状態
lgn          ← グループ名（空）
ly0 11       ← レイヤ0の状態
lyn          ← レイヤ名（空）
ly1 01
lyn
...（16グループ×16レイヤ）
```

### 4-3. 状態値の定義（`JWW_SMPL.BAT` より）

`ly%x %d` / `lg%x %d` の `%d` は2桁。

| 値 | 意味 |
|---|---|
| `11` | 編集可能、表示状態 |
| `01` | 編集不可、表示状態（＝表示のみ） |
| `00` | 編集不可、非表示状態（＝非表示） |

10の位が2以上はプロテクト：2=編集不可プロテクト(変更可)、3=編集可能プロテクト(変更可)、
6=編集不可プロテクト(変更不可)、7=編集可能プロテクト(変更不可)。

**書込レイヤは状態値ではなく、ヘッダ部の値なし `lg*` / `ly*` で示される。**

調べようとしていた4状態の対応：非表示=`00` / 表示のみ=`01` / 編集可能=`11` /
書込=ヘッダの値なし行。

### 4-4. 外部変形では状態を書き戻せない（実証済み）

`JWW_SMPL.BAT` の読み込みデータ仕様では、`lg%x` / `ly%x` は
**書込レイヤグループ・書込レイヤの変更のみ**。状態を設定する記法は無い。

実機で確認した。Jw_cad へ次を返した：

```
lg0
ly2 00      ← レイヤ2を非表示にできるか
ly5
```

結果：書込レイヤは `lgf/ly0` → `lg0/ly5` に変わった（＝データは読まれている）が、
レイヤ2の状態は `11` のまま。**`ly2 00` は無視された。**

### 4-5. 復元は レイヤ設定ファイル(.JWL) で行う（`SAMPLE.JWL` より）

```
PRTCT_CH =  1
LAYCND_0 =[G],(0),(1),(2),...,(F)
LAYCND_1 =...
...
LAYCND_F =...
```

- 先頭がレイヤグループ自身の状態、続く16個が各レイヤ
- `100`以上 = 書込レイヤグループ／書込レイヤ
- 1の位：`0`=非表示 / `1`=表示のみ / `2`=編集可能
- 10の位：`1`=プロテクト(状態変更可) / `2`=プロテクト(状態変更不可)
- `PRTCT_CH = 1` でプロテクト状態も変更可能になる
- 行頭 `#` は注釈。`END` を行頭に書くとそれ以降は無視される
  （`SAMPLE.JWL` は2行目付近に `END` があるので読んでも何も起きない）

読み込み方：[設定]→[環境設定ファイル]→[読込み]→ファイルの種類を `*.JWL` にして選択。

**外部変形側と値の体系が違うので変換が必要**（`dump_layers.py` の `to_jwl()` が担当）：

| `#gn` | JWL |
|---|---|
| `11` 編集可能・表示 | `2` |
| `01` 編集不可・表示 | `1` |
| `00` 非表示 | `0` |
| プロテクト 10の位 2,3 | 10の位 `1` |
| プロテクト 10の位 6,7 | 10の位 `2` |
| 書込レイヤ／グループ | `100` |

### 4-6. 外部変形をキー一発で起動する（`Sample.jwf` より）

環境設定ファイルに登録すると `[Ctrl]+[A]`〜`[Z]` で外部変形番号100〜125を直接実行できる。

```
GCOM_100 =A_SAVE,,,,,,,,,,C:\jww\JwNavigator
          ↑1番目=番号100=Ctrl+A          ↑11番目=フォルダ名
```

10ファイルごとに `GCOM_100`/`GCOM_110`/…/`GCOM_190` へ記述。拡張子は省略。
`[Ctrl]+[Shift]+[A]`〜`[Z]` は番号150〜175。

この設定はプロフィールに保存されないので、常用するなら `Jw_win.jwf` に書く必要がある。

**現在は適用していない**（一度入れたが元に戻した）。必要になったら上記1行を追記する。
バックアップ: `Jw_win.jwf.bak_20260904` / `kamo.JWF.bak_20260904`

注意: `[Ctrl]+[A]` に元から割り当てがある場合は上書きされる。空いている文字を選ぶこと。

## 5. 制約（回避策が見つからなかったもの）

**図形が1つも選択できないと外部変形が実行されない。**
`#h0` / `#h4`（データ選択をしない）にすると `#g1` が効かず16レイヤしか取れないため、
必ず範囲選択が必要。空の図面や、図形の無い領域を選んだ場合は起動しない。

ただし9章の【前範囲】が使えれば、初回以降はドラッグ自体が不要になる。

## 6. ファイル（すべて `C:\jww\JwNavigator`）

| ファイル | 役割 |
|---|---|
| `A_SAVE.bat` | 保存。`#jww #h1 #g1 #gn`。**本番用・唯一の安全な方法** |
| `dump_layers.py` | 解析本体。CSV/JSON/JWL を生成 |
| `B_SAVE_H4.bat` | 検証用（`#h4`）。16レイヤしか取れないので不要 |
| `C_SAVE_H0.bat` | 検証用（`#h0`）。未実行 |
| `D_SETTEST.bat` + `set_layer_test.py` | 書き戻し可否のテスト。結論は4-4 |
| `README_layerdump.md` | 使い方 |
| `layerdump\` | 出力先 |

`dump_layers.py` の出力：

- `raw_*.txt` … `jwc_temp.txt` の内容（2MB超のときはレイヤ行のみに間引く）
- `layers_*.csv` … 256行の一覧
- `matrix_*.csv` … 16×16の一覧表（人が見る用）
- `layers_*.json` … 差分用
- `commands_*.csv` … 行頭コマンドの出現数
- `restore_*.jwl` … 復元用（タイムスタンプ付き）
- `C:\jww\LAYER_RESTORE.JWL` … 復元用（常に最新）
- `log_*.txt` … 実行ログ

Jw_cad の画面には `he` 行で結果が出る：
`[A] group=16 layer=256 write=lgf/ly0  JWL保存OK`

## 7. 次にやること

1. **`.JWL` での復元が実際に効くか検証**（最重要）
   - `A_SAVE.bat` 実行 → レイヤを変更 → `LAYER_RESTORE.JWL` 読込 → 戻るか
   - `.JWL` はバージョン2.20頃の機能。以後の変更履歴に記載が無く、
     10.03 で仕様どおり動くかは未確認
2. **9章（前範囲 / Shift+V）の検証** — 操作数がここで決まる
3. **`[Ctrl]+[A]` の登録**（4-6）。やるかどうかは 2 の結果次第
3. 1が通ったら JwNavigator に統合
   - 保存：外部変形の呼び出し（または `[Ctrl]+[A]` 送出）
   - 復元：`.JWL` 読み込みの自動化。メニュー操作なのでキー割付できるか要調査
   - 複数スロット（名前を付けて複数の状態を保存／選んで復元）
4. 1が通らない場合の代替
   - Jw_cad のレイヤバー／レイヤ一覧ウィンドウへのクリック送出（UI操作）
   - ウィンドウ位置に依存するので脆い。最後の手段

## 8. ハマりどころ

- **`REM #jww` が無い bat は外部変形の一覧に出ない。** ファイルは存在しているのに
  選べない、という形で現れるので気づきにくい
- **`#g1` は `#h*` の直後**に書かないと無効（`JWW_SMPL.BAT` に明記されている）
- **`#cd` を使うと `jwc_temp.txt` の場所がずれる。** Jw_cad は自分が書き出した場所を
  読み直すので、返信は必ず読んだのと同じパスへ書くこと
- **256レイヤ揃わないときは `.JWL` を書いてはいけない。** 取れなかったグループを
  既定値で埋めてしまい、復元時に他グループの状態を破壊する
  （一度これで `LAYER_RESTORE.JWL` を壊した。現在はガード済み）
- **選択確定を押さないと bat は起動しない。** 「実行したのに出力が無い」の大半はこれ
- `jwc_temp.txt` は Shift_JIS(cp932)。bat も Shift_JIS + CRLF
- 図面全体を範囲選択すると `jwc_temp.txt` が巨大になる（対象図面は9.7MB）

---

## 9. 追記：操作を減らす手（変更履歴から判明・未検証）

`Jw_cad.chm` の VersionHistory に、範囲選択の手数を減らせる記述があった。

### 9-1. 選択確定はキーでできる

> Version 8.20 (2020/10/04)
> ・[Shift]＋V　を押すと、範囲確定になるようにした。
> 　（[基本設定]→[KEY]タブ　コマンド#156）

> Version 8.21 (2020/10/11)
> ・図形選択のとき、Enterキーを押すと範囲確定、Shift+Enterキーを押すと基準点変更となるようにし…

**[Shift]+[V] または [Enter] で選択確定できる。** コントロールバーを押しに行く必要がない。

### 9-2. 外部変形の範囲選択に「前範囲」「全範囲」ボタンがある

> Version 8.12 (2020/08/25)
> ・外部変形で、「#h1」により範囲内データ選択をするとき、「前範囲」「全範囲」が指定できるようにした。
> ・範囲選択から、外部変形の「#h1」による範囲内データ選択に移行した場合、選択範囲が継続するようにした。

**【前範囲】を押せば前回の選択範囲がそのまま再選択される。** ドラッグ不要。

この記述は `#h1` についてのもの。`#h2` でも同じボタンが出るかは不明なので、
`A_SAVE.bat` は `#h1` に戻してある。

想定される最短手順（未検証）：

```
[Ctrl]+[A] → [前範囲]クリック → [Shift]+[V]
```

初回だけ小さい範囲をドラッグしておけば、以後はドラッグ不要になるはず。
【全範囲】は図面全体が選ばれて jwc_temp.txt が巨大になるので使わないこと。

### 9-3. 選択範囲の記憶（8スロット）

範囲選択中に［属性選択］や［選択確定］ボタンを右クリックすると選択範囲が記憶され、
［前範囲］［全選択］ボタンの右クリックで呼び戻せる（Shift/Ctrl の組合せで8スロット）。
「前範囲」が他の操作で上書きされてしまう場合はこちらを使う。

### 9-4. 検証してほしいこと

1. 外部変形（`#h1`）の範囲選択で【前範囲】ボタンが出るか
2. [Shift]+[V] または [Enter] で選択確定できるか
3. 【前範囲】が Jw_cad 再起動後も残るか（残らなければ 9-3 の範囲記憶を使う）
4. `#h2` でも【前範囲】が出るなら、`#h2` の方が初回の狙いが楽

---

## 10. 無操作での保存（`E_AUTOSAVE.bat`）— 2026-09-04 追加・**実機未検証**

6.5 の目印点案は不要になる可能性がある。範囲選択そのものを回避する道が見つかった。

### 発想

実証済みの事実3つを組み合わせる。

1. `#h4`（データ選択をしない）でも `#gn` があれば**書込レイヤグループの16レイヤは取れる**（B の結果）
2. 書き戻しで**書込レイヤグループの変更 `lg%x` は効く**（D の結果）
3. 書き戻しの `hr` で**同じ外部変形を再実行できる**（`JWW_SMPL.BAT`、および
   Version 2.23 の「外部変形の JWC_TEMP.TXT 内の h/****.BAT と hr を有効にした」）

→ グループを1つずつ移しながら16周すれば、**範囲選択なしで256レイヤが取れる。**

```
1周目  : 現在の書込グループの16レイヤを取得 → `lg<次>` + `hr` を返す
2〜16周: 同上
最終   : 出力とJWLを書き、`lg<元>` `ly<元>` を返して書込レイヤを元に戻す
```

ユーザー操作は「外部変形を起動する」だけ。`[Ctrl]+[A]` に登録すれば**1キーで完了**。

### 実装

- `E_AUTOSAVE.bat` … 制御行は `#jww` `#h4` `#gn` `#e`（`#g1` は選択が無いと無効なので不要）
- `loop_dump.py` … 巡回の制御。解析・出力・JWL生成は `dump_layers.py` を再利用
- 途中経過は `layerdump\_loop_state.json`。120秒より古い状態は破棄して最初からやり直す
- 無限ループ防止：訪問済み・試行済みのグループを記録、`MAX_ITER = 40` で打ち切り

疑似応答による16周シミュレーションで、256レイヤ取得・書込レイヤ復帰・JWL生成まで確認済み。
ただし**実機の Jw_cad では未検証**。

### 検証してほしいこと

1. `E_AUTOSAVE.bat` を実行して `he [LOOP] group=16 layer=256 OK` が出るか
2. 実行後、書込レイヤグループ・書込レイヤが元に戻っているか
3. 16周にかかる時間（python の起動が16回走る。数秒の見込み）

### ⚠️ 2026-09-04 実機テストで発覚した設計上のバグ(kamo指摘、重要)

13周でクラッシュした件とは別に、**そもそも`lg%x`(グループ自身の状態)の値が正しく
取れていない**ことが判明した。実機で以下を確認:

- ユーザーはグループ5〜7のいずれかを**グループ自体として**非表示にした記憶があった。
- しかし復元後、`_loop_state.json`の`gstates`は捕捉できたグループ(0〜c)すべてで`11`
  (編集可能)均一だった。グループ内の16レイヤの値(`ly%x`)は記憶通り正しく復元できていた
  (グループ6=表示のみ、グループ7=非表示など)ので、**レイヤ単位の読み取りは正しいが、
  グループ単位の読み取りだけがおかしい**。

**原因の仮説(kamo提示、有力)**: このメモの2026-08-27の記録に「入力(書込み)から外れた
レイヤは自動的に編集可状態に落ち着く」という実測済みの挙動がある。同じ仕組みが**グループ**
にも当てはまるなら、`loop_dump.py`が各グループを順番に「書込みグループ」に切り替える
(`lg%x`書き戻し)という手法そのものが、**切り替えた瞬間にそのグループ自身の状態を
編集可(`11`)にリセットしてしまっている**可能性が高い。つまり「読み取るために書込み対象に
切り替える」という操作自体が、読み取りたい値(グループの本来の状態)を破壊してしまう
自己矛盾がある。

レイヤ側の値が正しく読めているのは、グループを書込み対象にしてもグループ**内の各レイヤ**の
状態(表示/非表示/表示のみ)は書込みレイヤの選択とは独立して保持されるためと考えられる
(こちらは既存の`utils/line_attr_dialog.py`の知見とも整合する)。

**対策の方向性(未検証)**: グループNを書込み対象にする直前に、まだそのNが書込み対象で
なかった時点の`lg%x`の値を(他のグループがアクティブな状態から)読んでおく必要があるかも
しれない。例えば「グループNに切り替える直前の`jwc_temp.txt`に載っているlgN行の値」を
使う、切り替え前後で2回読む、などの方式変更が必要。次回`loop_dump.py`を直す際は、まず
グループを実際に非表示にした状態を作ってから、切り替え前後で`lg%x`の値がどう変化するかを
単体で検証すること。

### 想定される失敗と対処

- **`hr` が効かない** … 1周で止まる。`log_E.txt` に `iter 1` だけが残り、
  書込レイヤグループが変わったままになる。手動で戻すこと。
  この場合は 9章（前範囲 / Shift+V）の手動ルートに戻る
- **プロテクトされたレイヤグループがある** … そのグループは書込状態にできないため取得できない。
  `missing=...` として報告され、**JWL は作られない**（不完全な JWL で復元すると破壊するため）
- **16周が遅い** … `loop_dump.py` を常駐プロセス化するなどの最適化余地はあるが、まずは計測から

### 復元側

復元は `.JWL` の読み込み（[設定]→[環境設定ファイル]→[読込み]）。
こちらはメニュー操作なので、無操作化するならメニューコマンドの送出か
キー割付が要る。コマンド番号があるかは未調査。

→ **解決済み**: メニューコマンドは idCommand=**32923**(「読込み(&R)」)。JwNavigator側から
`SendMessage(hwnd, WM_COMMAND, MAKELONG(32923,0), 0)`で呼び出すと、**標準のWindows「開く」
ダイアログ**(owner-drawではない通常のコモンダイアログ)が開くことを実機確認済み。ファイル名の
`Edit`コントロールに`WM_SETTEXT`でフルパスを設定→「開く」ボタンを`BM_CLICK`、で無人実行できる。
実際に`LAYER_RESTORE.JWL`を読み込ませ、書込みグループ/レイヤが指定通り(`(0,5)`)に切り替わる
ことを確認済み。**復元は完全自動化できる。**

### ⚠️ 2026-09-04 実機テストで Jw_cad がクラッシュ(重要な安全上の注意)

`E_AUTOSAVE.bat`(`#h4`+`hr`による16周自動ループ)を実機(kamo環境、Jw_cad 10.03.3)で
初めて実行したところ、**Jw_cad本体が落ちた**(実証用の図面だったため実害は無かったが、
本番ファイルで起きれば危険)。原因はまだ特定できていない(`hr`の応答形式が想定と違う、
`loop_dump.py`が書き戻す内容に問題がある、16回連続の`jwc_temp.txt`読み書き競合、等の
可能性がある)。

**現時点の結論**:
- **`hr`による自動ループ方式(`E_AUTOSAVE.bat`/`loop_dump.py`)は、原因を特定し安全性を
  確認するまで実データに対して使用しないこと。** 疑似応答でのシミュレーションは通っていたが、
  実機のJw_cadとの相性で問題が起きた。
- 代わりに、**`A_SAVE.bat`(`#h1`による手動範囲選択方式)は実機で複数回安定して動作しており、
  安全な現行の方法として維持する。** 6.5節の「ボタン作成時に固定の目印点を作る」案、または
  9章の「前範囲」「Shift+V」で手数を減らす方向を優先的に検証すること。
- 次に`hr`ループを試す際は、**必ず実証用ファイルで、かつ保存済みの状態から**行うこと。
  途中経過(`_loop_state.json`)や`log_E.txt`が残っていればクラッシュ原因の手がかりになる
  可能性があるので、再現時は確認すること。

**実際に残っていた手がかり(このクラッシュ時のログ)**:
- `layerdump\log_E.txt`: `iter 13 write group = c rows = 16` → `-> next group d` で止まっている。
  つまり**グループ0〜c(13グループ)までは正常に取得完了**しており、14周目(グループd、
  0始まりで14番目)への切替処理中にJw_cadが落ちた。
- `layerdump\_loop_state.json`: `"tried": ["0","1",...,"c","d"]`、`"iter": 13`。グループdへの
  遷移を試みた形跡があり、ログと整合する。
- ユーザー(kamo)の観察: 「13まではcmdが見えた」(コンソールウィンドウの点滅がiter 13まで
  確認できた)。
- **原因の当たり**: 13〜14回という比較的多い回数の連続した`外部変形`実行(`python`起動+
  `jwc_temp.txt`の読み書き)のどこかで、タイミング/競合が原因の可能性が高そうに見える
  (14回目特有の要因があるかは不明、単に連続実行回数が増えるとリスクが上がる可能性も)。
  次回調査時は、この2ファイルをまず参照すること。

---

## 11. 結論：`hr` ループ方式は破棄する（2026-09-04）

kamo が指摘した「グループ状態が全部 `11` になる」件は、**バグではなく方式そのものの欠陥**。
直せない。以下が根拠。

### なぜ起きるか

**書込レイヤグループは、定義上「編集可能・表示」でなければならない。**

- `SAMPLE.JWL`：「１の位にレイヤグループまたはレイヤの表示・編集状態を記述する。
  （**書込みレイヤグループまたはレイヤは除く**）」
  → 書込は状態指定の対象外。非表示の書込グループという状態は存在しない。
- `JWW_SMPL.BAT`：「書込レイヤグループの変更（プロテクトレイヤグループ・プロテクトレイヤは
  書込状態にできない）」
- 実測：巡回した13グループすべてが `11` 均一だった。

つまり `loop_dump.py` の「読むためにそのグループを書込に切り替える」という手段が、
**切り替えた瞬間に読みたい値を書き換えている。** 読み取り方法が観測対象を破壊している。

### これは読み取りエラーではなく、図面状態の破壊

さらに悪いことに、一度書込にしたグループは、書込から外れた後も `11`（編集可能）のまま残る。
つまり **`E_AUTOSAVE.bat` を実行すると、巡回したすべてのレイヤグループが
「編集可能・表示」に書き換えられる。** 保存しようとしている当の状態を消してしまう。

今回は実証用ファイルだったので実害は無かったが、**本番図面で実行すると
レイヤグループの表示状態がすべて失われる。**

### 修正案は成立しない

「切り替える直前の `lgN` の値を読む」案は使えない。`#h4` の出力には
**書込グループの `lg` 行しか出ない**（B の実測：groups=1）。他グループの状態は
そもそも出力されていないので、切り替え前に読むことができない。

全グループの `lg` 行が出るのは `#g1`＋実データ選択のときだけ。
それは `A_SAVE.bat` がやっていることで、その場合ループは不要。

### 対応

- **`E_AUTOSAVE.bat` と `loop_dump.py` は削除する。** 落ちるからではなく、壊すから。
- 保存は `A_SAVE.bat`（`#jww #h1 #g1 #gn`）が唯一の正しい方法。
  1回の実行で256レイヤとグループ状態を、**何も変えずに**取得できる。
- クラッシュ（iter 13→14）の原因は未特定のまま。方式を捨てるので追わない。

### 補足：この事故で学んだこと

`E_AUTOSAVE` を実行した図面のレイヤグループ状態は書き換わっている。
実行前の状態を記録した `matrix_*.csv` があれば、そこから `.JWL` を作って復元できる。

## 12. 無操作化の方針（10章を受けて）

復元側は解決済み（`WM_COMMAND` idCommand=32923 → 標準の「開く」ダイアログ →
`WM_SETTEXT` でパス投入 → `BM_CLICK`）。実機確認済み。

**同じ手法を保存側にも使えるはず。** 座標に依存しない経路がある：

1. `外部変形` コマンドを `WM_COMMAND` で送る（idCommand は未調査）
2. 開く「ファイル選択」ダイアログに `A_SAVE.bat` のパスを `WM_SETTEXT` → `BM_CLICK`
   （復元側で成功した手順とほぼ同じ）
3. 範囲選択状態になったら、**コントロールバーの【全選択】ボタンを `BM_CLICK`**
4. **【選択確定】ボタンを `BM_CLICK`**（または `[Shift]+[V]` / `[Enter]` を送る）

3・4 がボタンのクリックで済めば、**画面座標もドラッグも一切不要**。
6.5 節の目印点も不要になる。

### 検証してほしいこと

1. 外部変形コマンドの idCommand（Spy++ 等かメニュー列挙で取得）
2. 範囲選択中のコントロールバーの【全選択】【選択確定】が子ウィンドウとして取得でき、
   `BM_CLICK` に反応するか
3. 【全選択】だと図面全体が対象になりデータ量が大きくなる。重い場合は
   `A_SAVE.bat` の `#h1` を **`#h3`（範囲内の「文字」データのみを選択）** に変えると
   書き出しが文字だけになる。ただし `#h3` で256レイヤが取れるかは未検証

---

## 13. 実機検証結果と最終実装（2026-09-04、kamo作業分）

### 12章の提案(1)(2)は不成立と判明

- idCommand=**32913**が「外部変形」メニューのコマンド(`data/commands_master.csv`の
  C061で既に判明済みだった)。`WM_COMMAND`で送ると「ファイル選択」ダイアログ
  (class `#32770`、タイトル「ファイル選択」)は開く。
- しかし**このダイアログのファイル名Edit欄はES_READONLY(読み取り専用)**だった
  (`GetWindowLong(GWL_STYLE)`で確認)。`WM_SETTEXT`で書き込んでも直後に空へ戻る。
  復元側(idCommand=32923)の「開く」ダイアログは通常のコモンダイアログでEdit欄に
  書き込めたが、**外部変形の「ファイル選択」は別物の独自コントロールで、この
  経路は使えない**と確定した。

### 代わりに採用した方式: 4-6節のキー割り付け（成功）

4-6節にあった「`GCOM_100`に登録すると`Ctrl+A`〜`Z`で外部変形を直接起動できる」を
実装・実機確認した。

- `Jw_win.jwf`・`kamo.JWF`両方の`GCOM_100`を編集し、10番目の枠(`Ctrl+J`)に
  `A_SAVE`を登録(`Ctrl+A`は「あまりメジャーじゃない文字で」というkamoの希望により
  避けた)。バックアップ: `Jw_win.jwf.bak_20260904b`/`kamo.JWF.bak_20260904b`。
- 登録後、idCommand=32923の「環境設定ファイル読込み」自動化(既存の確立済み手法)で
  `Jw_win.jwf`を読み込ませ、キー割り付けを実行中のJw_cadに反映させた(ファイルを
  編集しただけでは実行中のJw_cadには効かない)。
- `keybd_event`で`Ctrl+J`を合成送信したところ、**ファイル選択ダイアログを一切経由
  せずに`A_SAVE.bat`が直接起動**し、ステータスバーに範囲選択待ちのプロンプトが
  出ることを実機確認。これで12章が目指していた「ファイル選択を無くす」目的は
  別ルートで達成できた（残るのは「小さい範囲を1回ドラッグ→選択確定」のみ）。

### JwNavigator本体への実装完了

`utils/layer_snapshot.py`(新規)に上記2つの自動化をまとめた:

- `trigger_save(hwnd)`: `Ctrl+J`合成送信。送信直前の`LAYER_RESTORE.JWL`のmtimeを
  記録して返す(完了検知用)。出力先パスはハードコードせず、対象hwndの実行ファイル
  パス(`GetModuleFileNameEx`)から動的に算出(環境非依存)。
- `check_save_complete(pending)` / `finalize_save(pending, dest_path)`: mtime変化を
  検知したら、ボタン専用の保存先へコピー。
- `trigger_restore(hwnd, jwl_path)`: idCommand=32923→開くダイアログのEdit欄へ
  `WM_SETTEXT`→「開く」ボタンを`BM_CLICK`。

`utils/palette_config.py`に`kind="layer_snapshot"`ボタン種別を追加
(`new_layer_snapshot_button()`、`snapshot_id`ごとに`config/layer_snapshots/
<snapshot_id>.jwl`という専用ファイルを持つ)。`widgets/settings_window.py`の
「コマンドを追加」に「➕ レイヤ保存ボタンを作る…」を追加。`widgets/toolbar.py`/
`main.py`でクリック時の分岐(未保存なら保存フロー開始、保存済みなら復元)と、
`monitor_loop()`からの毎秒ポーリングで保存完了を検知する仕組みを実装。

**実機で一連の流れを確認済み**: パレットで「レイヤ保存ボタンを作る」→名前入力→
ボタン押下→(自動で)Ctrl+J送出→外部変形起動→ユーザーが小さい範囲をドラッグ→
選択確定→(自動で)JWL検知・保存・ボタン表示が「💾保存済」に変化→再度ボタン押下→
(自動で)復元実行、まで通しで成功。ログ:
```
[レイヤ保存] 外部変形を起動しました name=電灯配線図。...選択確定してください。
[レイヤ保存] 電灯配線図 を保存しました
[レイヤ復元] 電灯配線図 を適用しました
```

**残課題(次回以降)**:
- 保存の「再保存(上書き)」UIが無い(現状は`config/layer_snapshots/*.jwl`を手動で
  消すしかない)。
- 複数グループ/複数レイヤにまたがる「本当に何もない」新規レイヤグループの状態
  取得は未検証(常に何かしら図形がある前提)。
- `Ctrl+J`のキー割り付けはこのマシンの`Jw_win.jwf`/`kamo.JWF`にのみ適用済み。
  他マシン/新規インストール環境へ配布する場合、初回セットアップで同様の登録が
  必要(現状は自動化されていない)。
